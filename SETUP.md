# Быстрая установка системы справочника

## ✅ Что уже готово

- ✅ **Backend интегрирован**: `app/main.py` уже подключает роутер справочника
- ✅ **Bot интегрирован**: `bot.py` уже содержит кнопку "📚 Справочник"
- ✅ **Модели импортированы**: Таблицы БД будут созданы автоматически
- ✅ **Mini App готов**: UI для справочника уже доступен в `/static/reference.html`

## 🚀 Шаги для запуска

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Запустите приложение

```bash
# Запуск FastAPI (в одном терминале)
uvicorn app.main:app --reload --port 8000

# Запуск бота (в другом терминале)
python bot.py
```

### 3. Таблицы создадутся автоматически

При первом запуске FastAPI создаст 3 новые таблицы:
- `reference_spells`
- `reference_items`
- `reference_creatures`

### 4. Протестируйте кнопку

1. Откройте бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку "📚 Справочник"
4. Откроется Mini App

## ⚠️ Важно: Парсер нужно доработать!

Текущий парсер в `app/parsers/dndsu_parser.py` - **базовый шаблон**.

### Чтобы начать загружать данные:

1. **Изучите HTML-структуру** страниц на https://next.dnd.su/
   - Откройте страницу заклинания в браузере
   - Нажмите F12 (DevTools)
   - Найдите элементы с данными (название, уровень, школа, описание)
   - Запишите их CSS-селекторы или классы

2. **Обновите методы парсера**:

```python
# Пример улучшения в app/parsers/dndsu_parser.py

# Вместо:
name = soup.find('h1').text.strip()

# Используйте точный селектор:
name_elem = soup.find('div', class_='spell-header__name')
name = name_elem.text.strip() if name_elem else None
```

3. **Запустите загрузку тестовых данных**:

```bash
# Загрузить 3 заклинания для теста
python scripts/load_reference_data.py --type spells --limit 3
```

4. **Проверьте API**:

```bash
# Автодополнение
curl "http://localhost:8000/reference/search/suggestions?q=fire&limit=5"

# Список заклинаний
curl "http://localhost:8000/reference/spells/search?limit=10"
```

## 📖 Полная документация

- **Детальная инструкция**: [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md)
- **Описание функций**: [README_REFERENCE.md](./README_REFERENCE.md)
- **Pull Request**: [#2](https://github.com/Restpapi16/dnd_tracker/pull/2)

## 🎯 Следующие шаги

1. ✅ **Запустить и протестировать** UI справочника
2. ⚠️ **Улучшить парсер** под реальную структуру next.dnd.su
3. ⏳ **Загрузить данные** (после улучшения парсера)
4. 🎨 **Настроить UI** под свои нужды
5. 🚀 **Добавить фильтры и избранное**

## 💡 Советы

- Начните с **малого**: загрузите 3-5 заклинаний для теста
- **Не спешите** с массовой загрузкой до улучшения парсера
- Используйте **DevTools** браузера для изучения структуры страниц
- Добавьте **задержки** между запросами (1-2 секунды)

## 🆘 Помощь

Если что-то не работает:

1. Проверьте, что установлены все зависимости: `pip list | grep -E "httpx|beautifulsoup4|lxml"`
2. Убедитесь, что FastAPI запущен: `curl http://localhost:8000/health`
3. Проверьте логи приложения на наличие ошибок
4. Посмотрите детальные инструкции в [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md)

Удачи! 🎲
